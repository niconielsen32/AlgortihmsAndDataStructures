#include <bits/stdc++.h>

//#include "tree.h"
//#include "node.h"
#include "driver.h"

using namespace std;



int main(){


    cout << "Minimum steps: " << minStepToReachTarget(8, 8, 4, 4, 3, 2) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(1000, 1000, 0, 0, 999, 999) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(200, 200, 100, 5, 2, 199) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(500, 500, 0, 0, 4, 2) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(200, 200, 50, 199, 0, 0) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(1000, 1000, 900, 800, 100, 200) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(1000, 1000, 400, 0, 200, 800) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(8, 8, 4, 4, 3, 2) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(8, 8, 4, 4, 3, 2) << endl;

    cout << "Minimum steps: " << minStepToReachTarget(8, 8, 4, 4, 3, 2) << endl;


    return -1;
}



